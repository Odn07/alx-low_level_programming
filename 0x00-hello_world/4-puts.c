#include <stdio.h>

/**
 * main - Entry point
 *
 * Return: Always 0 (Success)
 */

int main(void)
{	char putsVal[] = "\"Programming is like building a multilingual puzzle";
	puts(putsVal);
	return (0);
}
